import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    content: {
        height: '100%',
        padding: 24,
        gap: 16,
    },

    or: {
        flexDirection: 'row',
        gap: 16,
    },

    detail: {
        paddingBottom: 16,
    }
  });